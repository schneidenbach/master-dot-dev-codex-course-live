# Auction House — Part 1

```sh
npm install
npm start
```

`npm start` ensures the shared PostgreSQL container and `auction_part_1` database are ready,
runs pending migrations, and starts both apps. Open <http://localhost:5101> (API `3101`).
