{
  "name": "auction-house",
  "private": true,
  "workspaces": ["server", "web"],
  "scripts": {
    "db:up": "../scripts/workshop-db up auction_part_1",
    "db:down": "../scripts/workshop-db drop auction_part_1",
    "db:reset": "npm run db:down && npm run db:up && npm run migrate",
    "migrate": "DATABASE_URL=postgres://auction:auction@localhost:55432/auction_part_1 npm run migrate --workspace server",
    "dev": "API_PORT=3101 WEB_PORT=5101 DATABASE_URL=postgres://auction:auction@localhost:55432/auction_part_1 concurrently -k -n server,web \"npm run dev --workspace server\" \"npm run dev --workspace web\"",
    "start": "npm run db:up && npm run migrate && npm run dev",
    "test": "DATABASE_URL=postgres://auction:auction@localhost:55432/auction_part_1 npm run test --workspace server && npm run test --workspace web"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
