{
  "name": "auction-house",
  "private": true,
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "db:up": "../scripts/workshop-db up auction_part_4",
    "db:down": "../scripts/workshop-db drop auction_part_4",
    "db:reset": "npm run db:down && npm run db:up && npm run migrate",
    "redis:up": "docker compose -f ../docker-compose.yml up -d --wait redis",
    "migrate": "DATABASE_URL=postgres://auction:auction@localhost:55432/auction_part_4 npm run migrate --workspace server",
    "dev": "API_PORT=3104 WEB_PORT=5104 DATABASE_URL=postgres://auction:auction@localhost:55432/auction_part_4 REDIS_URL=redis://localhost:56379 concurrently -k -n server,web \"npm run dev --workspace server\" \"npm run dev --workspace web\"",
    "start": "npm run db:up && npm run redis:up && npm run migrate && npm run dev",
    "test:bidding-war": "playwright test tests/bidding-war.spec.ts --headed --project=edge",
    "test": "DATABASE_URL=postgres://auction:auction@localhost:55432/auction_part_4 npm run test --workspace server && npm run test --workspace web"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "concurrently": "^9.2.1"
  }
}
